# Install with:  pip install -r requirements.txt
# (see README.md for the PyTorch line to run first if you have a GPU)
torch>=2.2
transformers>=4.40
peft>=0.10
sentence-transformers>=2.7
scikit-learn>=1.4
numpy>=1.26
pandas>=2.2
accelerate>=0.30
